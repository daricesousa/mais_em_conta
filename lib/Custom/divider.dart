import 'package:flutter/material.dart';

class DividerCust extends StatelessWidget {
  const DividerCust({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Colors.transparent,
    );
  }
}